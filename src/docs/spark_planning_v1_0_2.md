# ✦ Spark — 기획안 1.0.2

> Ai co-writing service

---

## 컨텍스트 인계

너는 전문적인 PM이자 개발자야. 본질에 집중하여 최대한 단순하고 간소화된 방향으로 개발해야 해. 내 의견을 항상 비판적으로 분석해야 해.

---

## 1. 제품 개요

### 핵심 가치
- **시작을 쉽게** — 글감이 질문이고 입력창이 답변 공간. 첫 글자를 치면 바로 에디터 페이지로
- **본질에 집중** — 자동저장 + 이어쓰기로 흐름 유지. 글쓰는 과정 몰입 유지
- **지속성** — 글이 누적되어 습관화. 아카이브에서 시각적으로 확인

### 궁극적 목적
글감에서 출발하되, 궁극적으로는 사용자가 자신의 생각을 구조화하고 표현하는 법을 글쓰기를 통해 연습하고 성장할 수 있게 만드는 것. 쓸수록 나에게 맞는 글감이 나오는 구조.

### 타겟 사용자

| 구분 | 내용 |
|---|---|
| 연령대 | 20대 중반 ~ 30대 중반 |
| 타입 1 | 스타트업 종사자 / 커리어 성장에 관심 있는 직장인 |
| 타입 2 | 창업을 희망하는 사람 (막연히 꿈꾸는 직장인 + 예비 창업자) |
| 타입 3 | 나의 내면을 잘 가꾸어내고 싶은 사람 |
| 페인포인트 | 내 생각을 구조화하는 데 어려움이 있어 글쓰기로 키우고 싶은 사람 |

### 비전 로드맵

| 단계 | 내용 |
|---|---|
| MVP (지금) | 온보딩 → 글감 추천 → 글쓰기 → 이어쓰기 → 피드백 → 아카이브 |
| v2 | 기능 추가 + 글감 알고리즘 고도화 + 글쓰기 경험 설계 최적화 |
| v3 | 글 공개 및 공유로 인한 네트워크 커뮤니티화 |

---

## 2. 기술 스택

| 역할 | 기술 | 상태 |
|---|---|---|
| 프론트엔드 + 백엔드 | Next.js (App Router, TypeScript, Tailwind) | ✅ 완료 |
| DB + 로그인 | Supabase | ✅ 완료 |
| AI 모델 | Claude API (claude-sonnet-4-6) | ✅ 완료 |
| 배포 | Vercel (spark-theta-one.vercel.app) | ✅ 완료 |
| 폰트 | Pretendard | 🔧 교체 필요 |

---

## 3. 화면 구조

### 전체 흐름

```
로그인 → 홈 → 에디터 → 아카이브
```

### 최초 진입 흐름

```
링크 접속
  ↓
로그인 — spark 브랜드 + 카피 + Google 버튼
  ↓ Google 로그인 (최초 1회)
홈 (첫 진입 — 글감 + 시각적 유도 CTA)
  ↓ 첫 글자 입력 즉시
에디터
  ↓ 저장
홈
```

> ⚠️ 온보딩 1단계(writing_motivation 선택) 제거. 로그인 즉시 홈 진입. 첫 진입 경험 자체가 온보딩.

### 반복 사용 흐름

```
홈 — 글감 카드 + 입력창
  ↓ 첫 글자 입력 즉시
에디터 — 글쓰기 + 자동저장 + 이어쓰기 + 피드백
  ↓ 저장 or 뒤로가기
홈 복귀 (자동저장)
```

### 아카이브 흐름

```
홈 상단 아카이브 아이콘
  ↓
아카이브 — 프로필 + 설정 + 글 목록 + 검색
  ↓ 글 탭
에디터 (수정 모드) — 저장 후 아카이브 복귀
```

### 네비게이션 원칙

- 탭바 없음. 홈 상단 우측 아카이브 아이콘 하나
- 에디터 복귀 분기: 새 글 작성 → 홈 / 기존 글 수정 → 아카이브

---

## 4. 온보딩 UX

### 구조
로그인 직후 홈 진입. 최초 1회만 해당. 이후 재진입 시 홈으로 바로 이동.

**설계 원칙:** 첫 글쓰기 경험을 빠르게 완료시키는 것이 목적. 진입 마찰 최소화.

> ⚠️ writing_motivation 선택 단계 제거. 온보딩 = 홈 첫 진입 경험으로 대체.

### middleware 처리

| 조건 | 처리 |
|---|---|
| 비로그인 | /login 리다이렉트 |
| 로그인 | /홈 |

> writing_motivation null 체크 로직 삭제. onboarding_completed 플래그 불필요.

### Spark 소개 글 (자동 생성)

홈 첫 진입 시점에 writings 테이블 자동 삽입. 글 작성 여부와 무관하게 생성.

| 항목 | 내용 |
|---|---|
| topic_id | null |
| topic_content | null |
| body | 아래 소개 글 참고 |
| is_system | true |
| 알고리즘 영향 | 없음 (카테고리 없음) |
| 삭제 | 가능. 모달 문구: "처음 쓴 글이에요. 지워도 괜찮아요." 버튼: [삭제할게요] [남겨둘게요] |

**소개 글 내용:**

```
Spark를 소개합니다. ✦

매일 다른 글감, 쓸수록 내 취향으로.

글이 막히면 AI가 다음 문장을 조용히 제안해요.
✦ 피드백 버튼으로 전체 흐름도 살펴볼 수 있어요.

글은 아카이브에 쌓여요.
쌓일수록 내 생각의 구조가 보여요.
```

---

## 5. 홈 UX

> ⚠️ MVP 범위: 기존 홈 코드 최소 수정. 글감 알고리즘 가중치 쿼리 적용이 핵심.

### 레이아웃

```
──────────────────────────────────────
OOO님, 안녕하세요   [아카이브 아이콘]
──────────────────────────────────────

[←]  잘하고 싶은 마음이 오히려 발목을 잡은 적 있나요?  [→]
           👍 👎

[떠오르는 장면부터 써봐요...]
──────────────────────────────────────
자유롭게 써볼게요
──────────────────────────────────────
```

### 확정 항목

| 항목 | 내용 |
|---|---|
| 상단 | 닉네임 인사 (Google 자동 연동) + 아카이브 아이콘 |
| 글감 전환 | [←] [→] 버튼 글감 좌우 배치. 스와이프 없음 — 좋아요/싫어요 터치 영역 충돌 방지 |
| 첫 진입 화살표 | 오른쪽 [→]만 노출. 글감 넘긴 후부터 [←] [→] 양쪽 노출 |
| 시각적 유도 | 입력창 pulse 애니메이션. 텍스트 설명 없이 시각적으로만 유도 |
| 글감 일일 제한 | 하루 20개. 초과 시 "오늘의 글감을 모두 봤어요. 내일 새로운 글감이 기다리고 있어요." + 자유롭게 써볼게요 버튼 |
| 에디터 전환 | 첫 글자 입력 즉시 전환 |
| 자유 주제 | 입력창 아래 텍스트 링크 "자유롭게 써볼게요" |
| 네비게이션 | 탭바 없음. 상단 아카이브 아이콘 하나 |
| 스트릭 | 홈에서 표시 없음. 아카이브에서 담당 |

---

## 6. 에디터 UX

### 기본 스펙

| 항목 | 내용 |
|---|---|
| 기기 | 모바일 우선 / 최대 너비 390px / 세로 스크롤 범위 설정 필요 / 상하단 세이프 영역 설정 필요 |
| 글감 위치 | 본문 위 — 스크롤 내려가면 자연스럽게 사라짐 |
| 글감 수정 | 가능 (원본 topic_id 고정 저장) |
| placeholder | writing_motivation에 따라 변경 |
| 글자 수 | 제한 없음 |
| 서식 | 볼드 / 이탤릭 툴바 최소한 / 글자 크기 조정 +, - 기호로 단순하게만. 키보드 위 고정 |
| 자동 저장 | 타이핑 멈추고 1.5초 후 디바운스 저장 |
| 저장 상태 | 헤더 우측 "저장 중..." / "✓ 저장됨" / "저장 실패" |
| 이탈 | 뒤로가기 → 즉시 저장 후 홈 복귀 |

### 이어쓰기

**핵심 경험:** 사용자가 글을 쓰다 멈추는 찰나에, AI가 다음 문장을 회색 텍스트로 조용히 제안. 쓰는 흐름을 깨지 않는 것이 최우선.

```
타이핑 멈춤 (3~4초)
  ↓
백그라운드 API 호출 (사용자 모름)
  ↓
커서 뒤에 회색 흐릿한 텍스트로 1문장 제안
  ↓
✓ 수락 → 한 글자씩 타이핑 효과로 본문 합류
× 거절 → 제안 텍스트 사라짐
타이핑 재개 → 제안 자동 소멸 (✗ 버튼과 동일하게 처리, 모바일 기준)
```

| 항목 | 내용 |
|---|---|
| 멈춤 감지 시간 | 3~4초 시작. 데이터 보고 조정 |
| 컨텍스트 범위 | 현재 문단 전체 + 직전 문단 첫 문장 |
| 제안 수 | 1문장 |
| 표시 | 커서 뒤 회색 흐릿한 텍스트 (opacity 낮게) |
| 수락 | 하단 고정 ✓ 버튼 (문구 없음) |
| 거절 | 하단 고정 × 버튼 (문구 없음) |
| 타이핑 재개 시 | 제안 자동 소멸. ✗ 버튼과 동일하게 처리 |
| 수락 애니메이션 | 한 글자씩 타이핑 효과 |
| 수락 후 쿨다운 | 10초. 이후 멈춤 감지 재활성 |
| 거절 후 | 다른 추천 버튼 없음. 제안 소멸 후 쿨다운 재시작 |
| 첫 등장 툴팁 | "AI가 다음 문장을 제안했어요. ✓ 수락 × 거절. 해당 기능을 원하지 않으면 아카이브에서 끌 수 있어요." → 탭하거나 3초 후 자동 소멸 |
| 이후 | 버튼만 표시 (문구 없음) |
| 최소 글 길이 | 제한 없음 — 짧은 글에서도 작동. 흐름 경험이 목적 |
| API 실패 시 | 조용히 무시. 다음 멈춤 때 재시도 |
| 비용 절감 | 현재 문단 + 직전 문단 첫 문장만 전송. max_tokens 100 |

### ✦ 피드백

| 항목 | 내용 |
|---|---|
| 트리거 | 에디터 하단 "✦ 피드백" 버튼. 50자 이상일 때 활성화 |
| 컨텍스트 | 글 전체 본문 |
| 결과 표시 | 논모달 바텀 시트 (드래그 가능. 배경 dimming 없음) |
| 기본 높이 | 화면 40%. 드래그업 시 80% |
| 비용 | 사용자가 원할 때만 호출 |
| 피드백 톤 | 공감 우선 → 평가 금지. 짧고 감정적인 글에도 "잘 됐다/못 됐다" 판단 없이 공감으로 시작 |
| API 실패 시 | "잠깐 문제가 생겼어요. 다시 시도해볼게요." + 재시도 버튼 |

---

## 7. 아카이브 UX

### 역할 정의
홈이 글쓰기에만 집중하는 공간이라면, 아카이브는 **홈에서 처리하지 않는 모든 것을 담는 공간**이다. 프로필, 설정, 통계, 내 글 목록, 검색이 모두 여기에 있다. 마이페이지처럼 보이는 것이 의도된 구조다.

### 레이아웃

```
──────────────────────────────────────
상단 — 프로필 섹션
  [Google 사진]  OOO님      로그아웃
  닉네임 수정 (연필 아이콘)
  총 N편 썼어요 · 이번 주 N편
  이어쓰기 제안  [토글 ON/OFF]
  피드백 제안  [토글 ON/OFF]
──────────────────────────────────────
하단 — 글 목록

  [검색창]

  제목 또는 첫 줄 + 미리보기 2줄    5월 6일
  제목 또는 첫 줄 + 미리보기 2줄    5월 4일
  제목 또는 첫 줄 + 미리보기 2줄    5월 1일
──────────────────────────────────────
```

### 프로필 섹션

| 항목 | 내용 |
|---|---|
| 사진 | Google 프로필 이미지 자동 연동 |
| 닉네임 | Google 이름 자동 연동. 연필 아이콘 탭 → 인라인 수정 → 확인/취소 |
| 통계 | "총 N편 썼어요 · 이번 주 N편". 주간 리셋 월요일. 미달성 페널티 없음 |
| 마지막 작성일 | "마지막으로 쓴 날: N일 전". 재진입 사용자 맥락 제공. 오늘 쓴 경우 표시 안 함 |
| 로그아웃 | 프로필 우측. 확인 모달 후 로그인 화면으로 |

### 글 목록

| 항목 | 내용 |
|---|---|
| 표시 방식 | 제목(없으면 첫 줄) + 미리보기 2줄 + 날짜 |
| 카테고리 표시 | 없음 — 카테고리는 백엔드 알고리즘에만 사용. 사용자에게 노출 안 함 |
| 탭 필터 | 없음 |
| 검색 | 글 10편 이상일 때 검색창 노출. writings.body + topic_content 전문 검색 |
| 정렬 | 최신순 고정 |
| 글 탭 | 에디터로 진입 (수정 모드). 저장 후 아카이브로 복귀 |
| 글 삭제 | 스와이프 or … 메뉴 → 삭제 확인 모달 |
| 소개 글 삭제 모달 | "처음 쓴 글이에요. 지워도 괜찮아요." 버튼: [삭제할게요] [남겨둘게요] |
| 빈 상태 | "아직 쓴 글이 없어요. 첫 글을 써볼까요?" + 홈 이동 버튼 |

### 에디터 복귀 분기

| 진입 경로 | 저장 후 복귀 |
|---|---|
| 홈 → 에디터 (새 글) | 홈으로 복귀 |
| 아카이브 → 에디터 (수정) | 아카이브로 복귀 |

---

## 8. 글감 알고리즘

### 개요
카테고리 16개는 일단 고정. 추후 확장. 사용자 행동 데이터로 자동 학습하여 개인화. 쓸수록 나에게 맞는 글감이 나오는 구조.

> ⚠️ MVP에서는 글감 자체가 미검증 상태. writing_motivation 기반 개인화 제거. 전체 랜덤 노출 + 행동 데이터 수집 시작.

### MVP 글감 노출 방식
- 전체 랜덤 노출 (가중치 없음)
- 좋아요 / 싫어요 / 에디터 진입 데이터 수집
- 본인이 쓴 글감은 제외 (user_seen_topics 기준)

### 행동별 점수 (v2 적용 예정)

```
에디터 진입    +2점  (중간 신호)
글감 좋아요    +1점  (직접 피드백)
글감 싫어요    -1점  (직접 피드백)
글감 넘김      +0점  (노이즈)
```

> ⚠️ MVP에서는 에디터 진입 +2 / 좋아요·싫어요 ±1만 구현. 멈춤 감지 신호는 v2.

### 글감 뽑기 쿼리 (v2 기준)

```sql
SELECT t.* FROM topics t
JOIN topic_categories tc ON t.id = tc.topic_id
LEFT JOIN user_categories uc
  ON tc.category = uc.category AND uc.user_id = $1
WHERE t.id NOT IN (
  SELECT topic_id FROM user_seen_topics WHERE user_id = $1
)
ORDER BY (random() * COALESCE(uc.score, 1)) DESC
LIMIT 20;
```

### 글감 노출 비율 (v2 기준)

| 비율 | 대상 |
|---|---|
| 70% | score 높은 카테고리 글감 |
| 20% | score 낮은 카테고리 글감 |
| 10% | 아직 경험 못 한 카테고리 글감 |

비율이 정확히 떨어지지 않아도 엄격하게 지킬 필요는 없음

### 로드맵

| 단계 | 내용 |
|---|---|
| MVP | 전체 랜덤 노출. 좋아요/싫어요/에디터 진입 데이터 수집 시작 |
| v2 | 실제 행동 데이터로 카테고리 패턴 확인 후 가중치 적용 |
| v3 | writing_motivation과 실제 행동 패턴 교차 분석. 진짜 개인화 구축 |

---

## 9. DB 스키마

### 전체 테이블

| 테이블 | 주요 컬럼 | RLS |
|---|---|---|
| users | id, email, nickname, writing_motivation, onboarding_completed | 본인만 |
| topics | id, content | 전체 공개 |
| writings | id, user_id, topic_id, topic_content, body, is_system | 본인만 |
| user_seen_topics | id, user_id, topic_id, expires_at, seen_at | 본인만 |
| topic_categories | topic_id, category | 전체 공개 |
| user_categories | user_id, category, score, click_order, is_removed, selected_at | 본인만 |

### v4.2 신규 / 변경 컬럼

| 테이블 | 컬럼 | 타입 | 의미 |
|---|---|---|---|
| users | nickname | text | Google 이름 자동 연동. 아카이브에서 변경 가능 |
| users | writing_motivation | text | 온보딩 선택값. complex / unclear / expression / curious. **선택값 (필수 아님)** |
| user_categories | score | integer | 알고리즘 가중치. 기본값 0 |
| writings | is_system | boolean | 시스템 자동 생성 글 여부. 기본값 false. Spark 소개 글에만 true |
| user_seen_topics | expires_at | timestamptz | 싫어요로 숨긴 글감 만료일. NULL이면 영구 제외 (글 작성한 글감). 30일 후 만료 (싫어요 글감) |
| user_seen_topics | seen_at | timestamptz | 글감을 넘긴 시각. 일일 제한(20개) 계산에 사용 |

### writings 테이블 비고
- `topic_id`가 null이면 자유 주제 글 또는 시스템 생성 글
- `topic_content`는 글감 스냅샷 — 원본 글감이 수정·삭제되어도 작성 당시 글감 보존
- `is_system = true`인 글은 삭제 시 별도 모달 문구 적용

### user_seen_topics 비고
- `expires_at = null` → 영구 제외 (글 작성한 글감, 글감 소진 후 리셋 대상)
- `expires_at = 30일 후` → 싫어요 누른 글감. 만료 후 자동 재노출
- 일일 제한 쿼리: `seen_at >= today` 기준으로 하루 20개 초과 여부 판단

### SQL 마이그레이션

```sql
-- users 테이블 컬럼 추가
ALTER TABLE users
ADD COLUMN nickname text,
ADD COLUMN writing_motivation text
  CHECK (writing_motivation IN ('complex', 'unclear', 'expression', 'curious'));

-- user_categories score 컬럼 추가
ALTER TABLE user_categories
ADD COLUMN score integer DEFAULT 0;

-- writings is_system 컬럼 추가
ALTER TABLE writings
ADD COLUMN is_system boolean DEFAULT false;

-- user_seen_topics 컬럼 추가
ALTER TABLE user_seen_topics
ADD COLUMN expires_at timestamptz DEFAULT NULL,
ADD COLUMN seen_at timestamptz DEFAULT NOW();
```

### 글감 일일 제한 쿼리

```sql
-- 오늘 넘긴 글감 수 확인
SELECT COUNT(*) FROM user_seen_topics
WHERE user_id = $1
AND seen_at >= CURRENT_DATE
AND seen_at < CURRENT_DATE + INTERVAL '1 day';
-- 20개 이상이면 더 이상 넘기기 불가
```

---

## 10. 기준점 (Standards)

### 서비스 톤앤매너

**한 줄 정의:** 있지만 나서지 않는다

| 항목 | 기준 |
|---|---|
| 경어 | 해요체 — 합쇼체 X / 반말 X |
| 온도 | 차분하고 따뜻함 — 열정적·명령적 금지 |
| 거리감 | 옆에 있지만 말 걸지 않는다 — 코치·선생님·AI 느낌 X |
| 문장 길이 | 짧고 끊어지는 리듬 |
| 느낌표 | 최소 1개 이하 |
| 금지 표현 | "~해보세요!" / "~하면 좋아요!" / AI 챗봇 말투 |
| 버튼 레이블 | 기능 설명 X → 유저의 심리·행동으로 표현 |
| 넛지 원칙 | 압박 X → 허락·공감으로 |

### 글감 톤앤매너

| 항목 | 기준 |
|---|---|
| 형태 | 2인칭 질문형 고정 |
| 어미 | "~한 적 있나요?" / "~이었나요?" / "~했나요?" |
| 길이 | 15~25자 이내 |
| 금지 | 번아웃·탈진·무기력·결핍·후회 전제 / 뻔한 자기계발 / 결론 내버리기 |

### 버튼·레이블 확정 문구

| 상황 | 문구 |
|---|---|
| 이어쓰기 수락 | ✓ |
| 이어쓰기 거절 | × |
| 피드백 버튼 | ✦ 피드백 |
| 저장 중 | 저장 중... |
| 저장 완료 | ✓ 저장됨 |
| 저장 실패 | 저장 실패 |
| 글감 소진 | 오늘의 글감을 모두 봤어요. 내일 새로운 글감이 기다리고 있어요. |
| 빈 아카이브 | 아직 쓴 글이 없어요. 첫 글을 써볼까요? |
| 소개 글 삭제 모달 | 처음 쓴 글이에요. 지워도 괜찮아요. |
| 소개 글 삭제 버튼 | [삭제할게요] [남겨둘게요] |
| 네트워크 오류 | 연결이 잠깐 끊겼어요. 다시 시도해볼게요. |
| API 실패 (피드백) | 잠깐 문제가 생겼어요. 다시 시도해볼게요. |

---

## 11. 카테고리 (16개 / 4축)

| 축 | 카테고리 | 이해 돕기 위한 카테고리 설명 |
|---|---|---|
| 비즈니스 (5개) | 커리어 | 일하는 나는 어떤가 |
| | 창업 | 내가 만들고 싶은 것 |
| | 비즈니스 인사이트 | 내 눈으로 해석한 비즈니스 현상 |
| | 기획 | 생각을 구조화하는 법 |
| | 경험 | 우리가 실제 세상에서 마주하는 서비스 |
| 감성 (4개) | 감정 | 내가 느끼는 것들 |
| | 크리에이티브 | 만들고 표현하는 것 |
| | 예술 | 아름다움을 발견하는 것 |
| | 취향 | 내가 좋아하는 것들 |
| 문화 (2개) | 컬처 | 사회와 문화의 흐름과 현상 |
| | 테크&트렌드 | 기술과 변화를 읽는 법 |
| 본질 (5개) | 철학 | 어떻게 살 것인가 |
| | 인간관계 | 사람 사이에서 나는 |
| | 루틴 | 무너지지 않는 일상을 설계하는 법 |
| | 라이프스타일 | 내가 추구하는 삶의 방식 |
| | 웰니스 | 건강한 나를 위한 몸과 마음 돌보기 |

---

## 12. 폴더 구조

```
src/
├── app/
│   ├── auth/callback/route.ts
│   ├── login/page.tsx
│   ├── editor/
│   │   ├── page.tsx
│   │   └── EditorClient.tsx
│   ├── archive/
│   │   └── page.tsx
│   ├── actions/
│   │   ├── onboarding.ts
│   │   ├── main.ts
│   │   └── editor.ts
│   ├── MainClient.tsx
│   └── page.tsx
└── lib/supabase/
    ├── client.ts
    └── server.ts

middleware.ts
```

---

## 13. 개발 진행 현황

### ✅ 완료
- 환경 세팅 (Next.js + Supabase + Google 소셜 로그인)
- DB 스키마 (6개 테이블 + RLS)
- 글감 생성 스크립트 (카테고리당 100개)
- auth/callback 리다이렉트
- Vercel 배포 (spark-theta-one.vercel.app)
- middleware.ts (접근 제어)

### ☐ 작업 순서 (v4.2 기준)

**P0 — 개발 시작 전**
1. DB 마이그레이션 — nickname, writing_motivation(선택값), score 컬럼 추가 / user_seen_topics에 expires_at 추가. writing_motivation NOT NULL 제약 없음
2. layout.tsx — 타이틀 "spark", 언어 "ko", Pretendard 폰트 적용
3. auth/callback/route.ts — Google 닉네임 자동 저장 로직 추가
4. middleware.ts — writing_motivation null 체크 로직 삭제. 로그인 → 바로 /홈

**P1 — 핵심 기능**
5. 홈 — 글감 좌우 화살표 배치. 첫 진입 시 오른쪽 화살표만 노출. 입력창 pulse 애니메이션
6. 에디터 — 글감 위치 변경 (고정 → 본문 위 스크롤 연동)
7. 에디터 — 이어쓰기 구현 (멈춤 감지 + 회색 텍스트 + ✓/× 버튼 + 타이핑 재개 시 자동 소멸 + 첫 등장 툴팁)
8. 에디터 — ✦ 피드백 구현 (버튼 + 논모달 바텀 시트)
9. 에디터 — 복귀 분기 처리 (새 글: 홈 / 수정: 아카이브)
10. 홈 — 글감 알고리즘: MVP는 랜덤 노출 + 행동 데이터 수집
11. 홈 — 글감 좋아요/싫어요 아이콘 추가
12. 아카이브 — 프로필 + 이어쓰기/피드백 토글 + 글 목록 + 검색 전면 재작성
13. 홈 첫 진입 시 Spark 소개 글 자동 생성

**P2 — 보완**
14. 글감 소진 UX — "모든 글감을 다 봤어요" + 다시 보기
15. 에디터 — 첫 진입 시 "자동으로 저장돼요" 한 줄
